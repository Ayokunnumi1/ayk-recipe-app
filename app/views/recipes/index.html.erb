<section class="recipe-list-container">
    <% @recipes.each do |recipe| %>
        <div class="recipe-content">
            <p> <link_to    <%= recipe.name %></p>
            <p> <%= recipe.description %></p>
        </div>
        <% end %>
</section>